<!-- Navigation Bar -->
<nav class="navbar navbar-fixed-top navbar-default">
	<div class="container">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#menu">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<a class="navbar-brand page-scroll" href="#page-top">
				<span>[PHOTO]</span>
			</a>
		</div>
		<div class="collapse navbar-collapse navbar-right" id="menu">
			<ul class="nav navbar-nav">
				{% if app.request.pathinfo == '/' or app.request.pathinfo == '/index' %}
					<li class="active lien">
				{% else %}
					<li class="lien">
				{% endif %}
					<a href="{{ path('sym_index') }}">
						<i class="fa fa-home sr-icons"></i> Home
					</a>
				</li>

				{% if app.request.pathinfo == '/about' %}
					<li class="active lien">
				{% else %}
					<li class="lien">
				{% endif %}
					<a href="{{ path('sym_about') }}">
						<i class="fa fa-bookmark sr-icons"></i> About
					</a>
				</li>

				{% if app.request.pathinfo == '/blog' %}
					<li class="active lien">
				{% else %}
					<li class="lien">
				{% endif %}
					<a href="{{ path('sym_blog') }}">
						<i class="fa fa-file-text sr-icons"></i> Blog
					</a>
				</li>

				{% if app.request.pathinfo == '/contact' %}
					<li class="active lien">
				{% else %}
					<li class="lien">
				{% endif %}
					<a href="{{ path('sym_contact') }}">
						<i class="fa fa-phone-square sr-icons"></i> Contact
					</a>
				</li>

				{% if app.request.pathinfo == '/galeria' %}
					<li class="active lien">
				{% else %}
					<li class="lien">
				{% endif %}
					<a href="{{ path('sym_galeria') }}">
						<i class="fa fa-image sr-icons"></i> Galeria
					</a>
				</li>
			</ul>
		</div>
	</div>
</nav>
<!-- End of Navigation Bar -->
